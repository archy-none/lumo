load os.write_file(path: str, content: str): void;
load repeat(text: str, count: int): str;
load concat(a: str, b: str): str;
load to_str(n: num): str;

overload repeat = str * int;

let i = 0;
while i < 100 loop {
    let path = f"{i}.txt";
    let content = "Hacked! " * 100;
    write_file(path, content);
    let i + 1
}
